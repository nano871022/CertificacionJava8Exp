<h1><b>Certificacion JAVA 8 Ejemplos</b></h1>
<p>
Contiene todas las pruebas realizadas para comprender OCA y OCP para preparacion del Exament de certificacion de java8 programer 1 y programer 2
</p>

<p>
Esto se realiza sobre sublime para aprender como realizar los import correctament el uso de packages y como compilar archivos
</p>

<code>
<p>javac -sourcepath <b>{src-filder}</b> -d <b>{classes-folder}</b> <b>.\{src-folder}\{package-folder}\{file-name}.java</b></p>

<p>java -classpath <b>{classes-folder}</b> <b>{package-name}.{file-name}</b></p>
</code>
<p>
Lo que realiza el primer comando es indicar la carpeta que contiene el codigo, despues con el -d se indica conde se dejara el compiado y por ultimo se indica que archivos se desea compilar
</p>
<p>
Lo que realiza el segundo comando es indica la carpeta dondese se encuentra el compilado y despues se indica el package seguido del arhivo compilado a compilar.
</p>
